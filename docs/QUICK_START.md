# 快速开始

## 🚀 5 分钟上手

### 1. 安装依赖

```bash
npm install
```

### 2. 开发模式（推荐）

```bash
npm run dev
```

然后：
1. 打开 Chrome 浏览器
2. 访问 `chrome://extensions/`
3. 启用"开发者模式"（右上角开关）
4. 点击"加载已解压的扩展程序"
5. 选择项目中的 `dist` 目录

### 3. 生产构建

```bash
npm run build
```

### 4. 打包发布

```bash
npm run build:zip
```

生成的 `excellent-master-extension.zip` 可以直接上传到 Chrome Web Store。

## 📝 修改代码

所有源代码在 `src/` 目录下：

- `config.js` - 配置文件（API、URL、调试开关等）
- `mock-loader.js` - Mock 数据加载
- `interceptor.js` - 请求拦截
- `dom-handler.js` - DOM 修改
- `data/mock-data.json` - Mock 数据

修改后：
- **开发模式**：自动重新构建
- **生产模式**：需要重新运行 `npm run build`

## 🔧 常用命令

| 命令 | 说明 |
|------|------|
| `npm run dev` | 开发模式（监听文件变化） |
| `npm run build` | 生产构建（压缩混淆） |
| `npm run build:zip` | 构建并打包成 ZIP |
| `npm run clean` | 清理构建文件 |

## 📦 构建对比

| 特性 | 开发模式 | 生产模式 |
|------|---------|---------|
| 代码压缩 | ❌ | ✅ |
| 代码混淆 | ❌ | ✅ |
| Source Map | ✅ | ❌ |
| Console.log | ✅ | ❌ |
| 文件大小 | 大 | 小 |
| 调试难度 | 容易 | 困难 |

## 🎯 下一步

- 阅读 [BUILD_GUIDE.md](./BUILD_GUIDE.md) 了解详细构建配置
- 阅读 [DEBUG_CHART.md](./DEBUG_CHART.md) 了解如何调试图表
- 修改 `data/mock-data.json` 自定义数据
- 修改 `src/config.js` 调整配置

## ⚠️ 注意事项

1. **首次使用**必须先运行 `npm install`
2. **开发时**使用 `npm run dev`，修改代码会自动重新构建
3. **发布前**使用 `npm run build:zip` 生成最终版本
4. **加载扩展**时选择 `dist` 目录，不是项目根目录
